package weekPackages.week4

object video7_List {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(127); 

val google = List("android", "Android", "galaxy", "Galaxy", "nexus", "Nexus");System.out.println("""google  : <error> = """ + $show(google ))}
  //val l = List("7", "3", "9", "2")
  //7 :: 3 9, 2)
  
  /*def sortRecursif(xs: List[Int]): List[Int] =
  if (xs.isEmpty) xs
  else insert(xs.head, sortRecursif(xs.tail))*/
}
