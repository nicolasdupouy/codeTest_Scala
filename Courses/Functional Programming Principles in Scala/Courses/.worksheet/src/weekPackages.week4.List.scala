package weekPackages.week4

object List {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(52); 
 val b = 1;System.out.println("""b  : Int = """ + $show(b ))}
//val l = List("7", "3", "9", "2")
  //7 :: 3 9, 2)
  
  /*def sortRecursif(xs: List[Int]): List[Int] =
  if (xs.isEmpty) xs
  else insert(xs.head, sortRecursif(xs.tail))*/
  //System.out.println(google)
  
  /*def iSort(xs: List[Int]): List[Int] = xs match {
  case List() => List()
  case y :: ys => insert(y, iSort(ys))*/
  }
  
}
